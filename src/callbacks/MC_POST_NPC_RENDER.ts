// import { bodyAnim } from "../items/active/suicide";

export function npcRender(_npc: EntityNPC, _renderOffset: Vector): void {
  /* if (
    npc.Type === ModEntityTypes.TSARAHBODY &&
    npc.Variant === ModEntityVariants.TSARAHBODY
  ) {
    // bodyAnim(npc);
  } */
}
